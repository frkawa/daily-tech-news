---
date: 2026-07-03
sources: [qiita, zenn, hacker_news]
article_count: 7
---

# 技術ニュース日報 2026-07-03

生成日時: 2026-07-03 03:20:10 JST

## 今日のハイライト

### ★★★★☆ [遅いのは SQL だと思ったら、別のところにあった](https://zenn.dev/dress_code/articles/postgres-distinct-on-nested-loop)

**ソース:** zenn
**Rubyistへの影響:** Rails アプリケーション層での不要なデータ取得と集計ロジックを DB に委譲することで、N+1 問題同様のパフォーマンス改善が可能。

- ダッシュボード集計カードの遅延は SQL そのものが原因ではなく、クエリ呼び出し方とデータ運用に問題があった。約7.3秒かかっていた処理を最適化。
- 全件データをアプリ側で取得してから filter().length で集計する実装を、DB の COUNT + GROUP BY に置き換え、転送データ量と処理を大幅削減。
- インデックスや統計が正常でも遅い場合、SQL チューニング前にクエリ呼び出し方と構造的な設計を疑うべき。段階的な切り分けが重要。

## Qiita

### ★★★☆☆ [Pub/Subの中でGeminiが動く？ AI Inference SMTでメッセージをリアルタイム加工してみた](https://qiita.com/yahanoyaha/items/215aac869388a7c71da8) `AI` `PubSub` `GoogleCloudNext` `GoogleCloud`

- Google CloudのPub/SubにAI Inference SMT機能が実装され、メッセージ処理中にGeminiなどのAIモデルで自動推論し結果をマージできるようになった。従来必要だった中継用Cloud Functionsが不要になる。
- AI Inference SMTはSMT（Single Message Transforms）ファミリーの一機能で、トピック側またはサブスクリプション側に設定可能。MaaS系モデル（GeminiやClaude）と自前デプロイモデルの両方に対応している。
- YAMLの設定1枚でメッセージの感情分析やデータ加工ができ、レート制限対応やデプロイなど周辺コードの実装が大幅に削減される。モデル呼び出しのオーケストレーションはPub/Sub側で自動処理される。

**Rubyistへの影響:** Rubyistに直接的な影響は限定的だが、GCPを使ったバックエンド構築でPub/Subを活用する際、Ruby Functionsの開発負荷が削減される可能性がある。

## Zenn

### ★★★★☆ [遅いのは SQL だと思ったら、別のところにあった](https://zenn.dev/dress_code/articles/postgres-distinct-on-nested-loop) `postgresql` `sql` `database` `performance`

- ダッシュボード集計カードの遅延は SQL そのものが原因ではなく、クエリ呼び出し方とデータ運用に問題があった。約7.3秒かかっていた処理を最適化。
- 全件データをアプリ側で取得してから filter().length で集計する実装を、DB の COUNT + GROUP BY に置き換え、転送データ量と処理を大幅削減。
- インデックスや統計が正常でも遅い場合、SQL チューニング前にクエリ呼び出し方と構造的な設計を疑うべき。段階的な切り分けが重要。

**Rubyistへの影響:** Rails アプリケーション層での不要なデータ取得と集計ロジックを DB に委譲することで、N+1 問題同様のパフォーマンス改善が可能。

### ★★★★☆ [優秀な人ばかりなのに、組織が重い ── AI時代に複利で膨らむ『認識負債』の話](https://zenn.dev/canly/articles/0c01a3124b8480) `ai` `マネジメント` `組織` `エンジニアリングマネージャー` `okr`

- 認識負債とは組織の「なぜ・何を・どこへ」の方向性のズレが複利で増える負債。技術負債と異なり見えにくく指摘しづらいため放置されやすい。
- 最初は小さなズレでも、前のズレが次の議論の土台となり、会議を重ねるたびにボタンの掛け違いが拡大していく構造を持つ。
- 同じ言葉・資料・会議でも参加者の頭の中で描く絵が微妙に異なり、全員が同じ地図を見ているつもりで違う場所へ向かう組織の状態。

**Rubyistへの影響:** チーム開発で暗黙的な認識のズレは生産性低下に直結するため、明示的なコミュニケーションと認識合わせの重要性が高まる。

### ★★★★☆ [Claudeに「オントロジー」を持たせたら、コスト半分・3倍速になるかも](https://zenn.dev/takupeso/articles/claude-ontology-knowledge-structuring) `ai` `claude` `ナレッジグラフ` `claudecode` `オントロジー`

- Claudeにコンテキストをフラット構造で与えると、概念間の関係が曖昧なため推測にブレが生じ、出力精度が安定しない問題がある。
- オントロジーは概念、インスタンス、プロパティ、関係、制約を明示的に定義し、モノとモノの繋がりを構造化する情報整理手法。
- オントロジーを導入することで、AIが概念間の関係を正確に把握でき、コスト削減と処理速度向上が期待できる。

**Rubyistへの影響:** Rails開発でマイクロサービス間の依存関係やアーキテクチャ設計をClaudeに正確に理解させることで、より精度の高いコード生成が可能になる。

### ★★☆☆☆ [AIエージェント時代のターミナルマルチプレクサ「herdr」にtmuxから乗り換えた](https://zenn.dev/studypocket/articles/herdr-ai-agent-multiplexer) `ai` `terminal` `tmux` `claudecode` `herdr`

- herdrはRust製のエージェントマルチプレクサで、複数のAIコーディングエージェントを1つのターミナルから管理することに特化している
- tmuxの永続セッション機能を保ちながら、エージェント状態の検知・可視化やCLI・Socket API による標準的なマルチプレクサ操作を提供する
- 言語不問のSocket APIベースプラグイン機構により、任意の言語でプラグイン開発が可能で、既存ターミナルエミュレータ上でそのまま動作する

**Rubyistへの影響:** Ruby開発者への直接的な影響は限定的だが、AI開発ワークフロー最適化の参考事例として言語横断的な関心の対象となる可能性がある。

## Hacker News

### ★★☆☆☆ [An American Privacy Emergency](https://scottaaronson.blog/?p=9902)

- 米国勢調査局が統計製品から雑音注入を禁止する方針転換を実施し、プライバシー保護と統計精度のバランスに関する議論が再燃している
- 差分プライバイシーの導入と廃止をめぐる政策の揺り戻しは、政府統計データの信頼性と個人情報保護の根本的なジレンマを露呈している
- この決定は2026年6月に行われ、統計データの利用と個人プライバシー保護のトレードオフについての認識を深める契機となっている

**Rubyistへの影響:** Rubyistには直接的な影響は限定的だが、政府データAPI やデータ分析ライブラリの信頼性に関わる潜在的な課題を示唆している

### ★★★☆☆ [Show HN: zkGolf – Competitive optimization of formally verified circuits](https://zk.golf/)

- ゼロ知識証明では計算を回路（多項式方程式）として表現する必要があり、制約数の最適化が重要である。
- LLMを用いて形式仕様から回路を自動生成し、正確性を証明しながら最適化することで手作業を削減できる。
- SHA-256の例では、LLMが制約数を削減する最適化案を提案し、証明できない場合は自動的に修正される仕組みが機能した。

**Rubyistへの影響:** Ruby開発者への直接的な影響は限定的だが、形式検証とLLMの組み合わせにおける検証パイプラインの構築方法に参考価値がある。

---

[前日のニュース](../../2026/07/2026-07-02.md) | [アーカイブ](../../archive.md)
