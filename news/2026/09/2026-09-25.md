---
date: 2026-09-25
sources: [qiita, zenn, hacker_news]
article_count: 12
---

# 技術ニュース日報 2026-09-25

生成日時: 2026-09-25 03:47:53 JST

## 今日のハイライト

### ★★★☆☆ [許可ルールを settings.json に書いても効かなかった。分かれ目はフォルダを信頼したかどうか](https://qiita.com/suwa_nobu/items/92d4de34077b9ae02fca)

**ソース:** qiita
**Rubyistへの影響:** Ruby開発者の影響は限定的だが、Claude Code でスクリプト実行時に予期しない承認エラーが発生した場合の対処法として参考になる。

- Claude Code でフォルダを信頼しない状態では、settings.json の permissions.allow ルールが無視される。--allowedTools や --settings パラメータでは同じルールが有効に機能する。
- 許可ルール効果測定には npm config get registry など承認が必須のコマンドが必要。echo や cat などは許可ルール無しでも実行されるため、ルール効果の検証に使えない。
- フォルダを信頼済みにするか、コマンドライン引数で許可ルールを渡すことで問題が解決する。settings.json だけでは未信頼フォルダで機能しない仕様になっている。

## Qiita

### ★★★☆☆ [Jev完全ガイド：GPTやClaudeとは違う「判断するAI」は何を変えるのか!?](https://qiita.com/ryosuke_ohori/items/72ec59a33f8efb9d008a) `LLM` `Jev` `Python` `AI` `AIエージェント`

- JevはTypeScript型を返すAIで、GPTやClaudeとは異なり、文章生成ではなく型付きの確率的意思決定に特化している。処理時間は70～500ミリ秒で高速。
- 従来のLLMの問題点を解決し、定義された型の外にある不正な値を生成できないため、JSONパース検証の手間が不要になる。
- 料金は入力100万トークンあたり0.042ドル（約6.3円）で出力料金無料。プログラムの状態と型定義された質問を並列処理で評価できる。

**Rubyistへの影響:** RubyのAPIサーバーでLLMの判断結果を確実に構造化データとして扱える点で、エラーハンドリングが大幅に簡潔になる。

### ★★★★☆ [Claude Code の拒否ルールを7つの形で測った。最新版でまだ4つ残っている](https://qiita.com/suwa_nobu/items/d21ff35b9329eecc6143) `AI` `生成AI` `ClaudeCode` `Claude` `Security`

- Claude Code 2.1.282でも権限拒否ルール(permissions.deny)にはまだ4つのバイパス方法が存在する。特にバイパスモードやワイルドカード検索で機密ファイルへのアクセスが可能。
- 2週間で7つのバージョン更新があったが、teeコマンドやrm -rfでの削除など一部は修正されたものの、evalやenv -C、Write()ルール、設定ファイル無視などは未解決。
- セキュリティ検証は自己申告に頼らず実際にファイル変更や内容返却を確認する必要があり、環境変数の剥離などの厳格な測定手法が重要。

**Rubyistへの影響:** Ruby開発者がClaude Codeを使用する際、権限設定で保護されていると思っていた機密ファイルやプロジェクト設定が実際にはアクセス可能な状態にあるリスクがある。

### ★★★☆☆ [許可ルールを settings.json に書いても効かなかった。分かれ目はフォルダを信頼したかどうか](https://qiita.com/suwa_nobu/items/92d4de34077b9ae02fca) `AI` `生成AI` `ClaudeCode` `Claude` `Security`

- Claude Code でフォルダを信頼しない状態では、settings.json の permissions.allow ルールが無視される。--allowedTools や --settings パラメータでは同じルールが有効に機能する。
- 許可ルール効果測定には npm config get registry など承認が必須のコマンドが必要。echo や cat などは許可ルール無しでも実行されるため、ルール効果の検証に使えない。
- フォルダを信頼済みにするか、コマンドライン引数で許可ルールを渡すことで問題が解決する。settings.json だけでは未信頼フォルダで機能しない仕様になっている。

**Rubyistへの影響:** Ruby開発者の影響は限定的だが、Claude Code でスクリプト実行時に予期しない承認エラーが発生した場合の対処法として参考になる。

### ★★☆☆☆ [Claude Opus 5.5 の「40%安い」は、キャッシュの当たり方で24%にも59%にもなった](https://qiita.com/suwa_nobu/items/870b1d6c5502aca68f55) `AI` `生成AI` `ClaudeCode` `Claude` `LLM`

- Claude Opus 5.5は「40%安い」とされるが、実測ではキャッシュ作成時24%、キャッシュ読み込み時59%と変動。公式発表の条件が不明確。
- Claude Code 2.1.277では認識されず、公開日と同じ2.1.280以降で使用可能。環境更新が必須。
- 出力トークンが多い仕事では32%コスト削減・21%速度向上だが、短出力では速度差がわずか2.6%と限定的。

**Rubyistへの影響:** Rubyistへの直接的な影響は薄いが、Claude Code利用者は環境更新してモデル選択の詳細測定が必要。

## Zenn

### ★★★★☆ [age40 - 30代でやってよかったこと](https://zenn.dev/dress_code/articles/2026-09-24_age-40) `ai` `career` `engineering`

- 30代後半でコード執筆から技術広報・DevRelへシフトしても、「伝える」「つなぐ」を通じて技術理解は深まり、技術との関係は維持できた
- 技術コミュニティに参加し続けることで、会社内の「当たり前」が相対化され、組織や業界の制約に応じた最適解が存在することを学べた
- 細部は忘れても経験に基づいた判断力は残り、AIで作れる量が増える時代には「何を、なぜ作るか」を決める力がより重要になる

**Rubyistへの影響:** エンジニア人生の長期キャリア設計において、コード執筆以外の活動も技術力維持の有効な手段であることを示唆している

### ★★☆☆☆ [みんなJevの話してる。やってないの俺だけ](https://zenn.dev/unsoluble_sugar/articles/jev-system-one-model-intro) `ai` `machinelearning` `llm` `typesafe` `jev`

- Jevは TypeSafe AI が開発した「System One モデル」で、LLMではなく即座な判断が必要な用途に特化した「賢いif文」のような関数呼び出しAI。
- 文章生成ではなく型付きの確率的判断を返すため、出力トークン無料で200倍高速という性能を実現。ハルシネーション問題を軽減している。
- OpenAI出身の創業者による約2年のステルス開発を経て2026年9月15日に公開され、合成データのみで学習している新しいAIカテゴリ。

**Rubyistへの影響:** Ruby アプリケーションに意思決定ロジックを組み込む際、従来の複雑な条件分岐の代わりに Jev の API を活用することで処理を簡潔化できる可能性がある。

### ★★★☆☆ [Claude Opus 5.5によるピクセルアニメーション生成の検証](https://zenn.dev/peoplex_blog/articles/1bc5c181ad19f0) `javascript` `canvas` `生成ai` `claude` `ピクセルアート`

- Claude Opus 5.5を検証するため、サンゴ礁の一日をテーマにしたピクセルアニメーションを単一HTMLファイルで生成。外部ライブラリなしのCanvas 2D実装で、複雑な生態系シミュレーションを実現した。
- 小魚の群泳、サメへの反応、昼夜による行動変化など、生き物ごとの自律的なルールをプロンプトで指定。AIが多層的な動作ロジックを1つの指示から生成できることを実証した。
- 470行43KBのHTMLファイルで、情報量の多い動的シーンを実現。昼夜90秒ループ、コースティクス表現、クラゲの発光など、視覚的な豊かさと生態系の複雑さを両立させた。

**Rubyistへの影響:** 直接的なRuby関連性はないが、AIによるコード生成の実例として、複雑なロジックをテキスト指示から実装する可能性を示す参考事例となる。

### ★★☆☆☆ [自宅サーバーのススメ](https://zenn.dev/porters_tech/articles/b2282728b35397) `docker` `linux` `自宅サーバー` `セルフホスティング`

- 古いパソコンを自宅サーバーとして再利用する方法を紹介。既存のハードウェアにLinuxをインストールするだけで、無料で自分や家族向けのサービスを提供できるサーバーが構築できる。
- 自宅サーバーは高性能なマシンを必要としない。Raspberry Pi程度の性能を想定して作られたアプリケーションが多く、一般的なノートPCでも十分に動作可能。CasaOSなどのフレームワークで簡単に始められる。
- 開発者にとっての学習メリットを強調。実際に手を動かしてLinuxやサーバー関連の技術に触れることで、座学では得られない実践的な知識が身につく。

**Rubyistへの影響:** Rubyで開発する場合のサーバー構築実践学習の機会として、自宅サーバー環境は開発スキル向上に有用である。

## Hacker News

### ★★☆☆☆ [Show HN: Make cursed fonts like Times New Bastard](https://bastardica.mitpit.com)

- OpenTypeのligature機能を悪用して複数フォントを混合し、ユーモラスなフォントを生成するツール
- Python in WASMを活用してクライアント側で高速処理を実現する技術デモ
- ブラウザ上で直接実行可能な軽量で実用的なWASM活用例

**Rubyistへの影響:** Ruby開発者への直接的な影響は限定的だが、WASMを用いたクライアント側処理の新しい可能性を示している。

### ★★★☆☆ [Show HN: Whiteboard (YC W26) – An open-source IDE for thoughtful software design](https://github.com/devdotfast/whiteboard)

- Whiteboard はオープンソースのデスクトップIDEで、人間とAIエージェントが共通のワークスペースでソフトウェア設計を協働できるツール
- CodeOSSをベースに構築され、仕様図やコード間の直接的なナビゲーション、LSPサポートなどVSCodeの機能を統合
- Rustで実装したセマンティックなAST対応の差分ビューアにより、関連するコード変更のみを表示できる

**Rubyistへの影響:** Rubyistにとって直接的な言語依存がないが、AIとの協働開発ワークフロー改善の参考になる可能性がある。

### ★★★☆☆ [Show HN: Air-gapped file encryption as self-decrypting HTML page](https://cms-sfx-demo.apeleg.com/)

- 自己復号化機能を持つHTMLページ単体でファイルを暗号化・復号化する仕組みを開発。インストール不要でブラウザのみで動作
- ネットワーク接続なしでも動作し、OpenPGP署名を保持したまま復号可能。opensslでも直接復号できる設計
- 完全に監査可能な構造で、ソースコード公開とPGP署名による再現性により信頼性を確保

**Rubyistへの影響:** Rubyistへの影響は限定的だが、セキュアなファイル共有ツール開発の参考になる可能性がある

### ★★☆☆☆ [Show HN: Koi.rest – watch some fish and regain your balance](https://koi.rest)

- 開発者のPaulがADHDと失業のストレスから、仮想的な禅庭園プロジェクト「Koi.rest」を創設した。
- JavaScriptが未知の技能だったため、AIを活用してプロトタイプを開発し、完璧を求めず実現可能性を優先した。
- 静かにコイを眺められるWebサービスとして公開し、ストレス軽減とコミュニティ構築を目指している。

**Rubyistへの影響:** 直接的なRuby開発事例ではないが、AIツール活用による開発効率化の実例として参考になる。

---

[前日のニュース](../../2026/09/2026-09-24.md) | [アーカイブ](../../archive.md)
