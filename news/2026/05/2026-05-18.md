---
date: 2026-05-18
sources: [zenn, hacker_news]
article_count: 4
---

# 技術ニュース日報 2026-05-18

生成日時: 2026-05-18 03:46:24 JST

## 今日のハイライト

### ★★★★☆ [Bun が 6 日で Rust に書き換わった件](https://zenn.dev/ashunar0/articles/55a669c10e6a8d)

**ソース:** zenn
**Rubyistへの影響:** Ruby エコシステムへの直接的な影響は限定的だが、AI コード生成の大規模採用と品質管理の課題を示す事例として参考になる。

- JavaScript ランタイム Bun が Zig から Rust への大規模移植を実施。AI（Claude）が 96 万行のコードを 6 日間で生成し、テスト 99.8% パスで main にマージされた。
- 単一 PR で 100 万行超のコードが追加される異例の事態。従来のコードレビュー物理量では 1.9 年相当の作業を 6 日で実行。GitHub の AI 検出システムも困惑し "AI slop" フラグを立てた。
- unsafe ブロックは 13,000 箇所超で安全性に懸念。創業者 Sumner も「全部破棄する可能性が高い」と発言。Anthropic 自社 AI で自社プロダクトを書き換えた構図。

## Zenn

### ★★☆☆☆ [XプレミアムプランでGrokとx_searchツールを使う](https://zenn.dev/robustonian/articles/x_premium_grok_search) `twitter` `hermes` `xai` `aiエージェント` `grok`

- XプレミアムプランでGrokとx_searchツールが利用可能。SuperGrok契約は不要で、hermes-agentのインストールと設定だけで動作する。
- hermes-agentはcurlコマンドで簡単にインストール可能。その後、hermes toolsコマンドでx_searchツールを有効化し、xAI OAuth認証を設定する。
- セットアップ時にSuperGrok Subscriptionの表示が出現するが無視してOK。xAI OAuthで認可URLにアクセスすることで認証が完了する。

**Rubyistへの影響:** Ruby開発者への直接的な影響は限定的だが、AIツール統合の参考となる認証実装パターンとして参考になる可能性がある。

### ★★★★☆ [Bun が 6 日で Rust に書き換わった件](https://zenn.dev/ashunar0/articles/55a669c10e6a8d) `ai` `rust` `zig` `bun` `anthropic`

- JavaScript ランタイム Bun が Zig から Rust への大規模移植を実施。AI（Claude）が 96 万行のコードを 6 日間で生成し、テスト 99.8% パスで main にマージされた。
- 単一 PR で 100 万行超のコードが追加される異例の事態。従来のコードレビュー物理量では 1.9 年相当の作業を 6 日で実行。GitHub の AI 検出システムも困惑し "AI slop" フラグを立てた。
- unsafe ブロックは 13,000 箇所超で安全性に懸念。創業者 Sumner も「全部破棄する可能性が高い」と発言。Anthropic 自社 AI で自社プロダクトを書き換えた構図。

**Rubyistへの影響:** Ruby エコシステムへの直接的な影響は限定的だが、AI コード生成の大規模採用と品質管理の課題を示す事例として参考になる。

### ★★☆☆☆ [Vercelがシステムプログラミング言語「Zero」を公開した！ 悔しい悔しい悔しい！](https://zenn.dev/tkithrta/articles/74b35b39c2bb5e) `rust` `vercel` `webassembly` `zig` `wasi`

- Vercelがシステムプログラミング言語「Zero」を公開。AIエージェント向けで、予測可能なメモリ管理と明示的エフェクトシステムを特徴とする言語。
- RustやZigと比較して、Zeroはより単純なレキシカル借用衝突追跡を採用し、隠れた割り当てやランタイムメタデータを排除する設計。
- Wasm/WASI対応で拡張子は.0。直接エミッターを使用するためCコンパイラが不要で、構造化されたコンパイラ出力を提供。

**Rubyistへの影響:** システムプログラミング領域の動向であり、Ruby開発者への直接的な影響は限定的だが、言語設計思想の参考価値がある。

## Hacker News

### ★★★☆☆ [Show HN: Semble – Code search for agents that uses 98% fewer tokens than grep](https://github.com/MinishLab/semble)

- 大規模コードベースでのエージェント検索効率を大幅改善。grepと比べて98%少ないトークン使用量で99%の検索品質を実現
- Model2VecとBM25をRRFとコード認識シグナルで融合。CPU上で実行可能で、典型的なリポジトリを250ms以内にインデックス化
- Claude Code、Cursor、Codexなどと統合可能なMCPサーバー。API不要でローカル実行、設定なしで利用開始できる

**Rubyistへの影響:** Rubyプロジェクト検索の高速化に寄与するが、主にAIエージェント開発者向けのツールため直接的な影響は限定的

---

[前日のニュース](../../2026/05/2026-05-17.md) | [アーカイブ](../../archive.md)
